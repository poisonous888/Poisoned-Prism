name: build zip
on: [ pull_request, push ]

env:
  NAME: poisoned-prism
  VERSION: 0.1

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v3
      
      - name: create full zip
        run: |
          zip -r ${{ env.NAME }}-FULL-${{ env.VERSION }}.zip . -x ".git/*" ".github/*" .gitignore README.md
          
      - name: create equipment zip
        run: |
          zip -r ${{ env.NAME }}-EQUIPMENT-${{ env.VERSION }}.zip . -x ".git/*" ".github/*" .gitignore README.md
      
      - name: capture zip
        uses: actions/upload-artifact@v7.0.1
        with:
          path: |
            ${{ env.NAME }}-FULL-${{ env.VERSION }}.zip
            ${{ env.NAME }}-EQUIPMENT-${{ env.VERSION }}.zip
          archive: false